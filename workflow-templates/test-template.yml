name: Test workflow template for Actions training with changes for today's class

on:
  push:
    branches: [ $default-branch ]

jobs:
  echo:
    runs-on: ubuntu-latest

    steps:
      - name: Run a one-line script
        run: echo "Hello this is a workflow template"
